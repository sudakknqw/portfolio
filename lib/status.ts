/**
 * Availability status shown at the bottom of the hero.
 * Edit these lines only — the component builds everything from them.
 *
 * Busy? Set `available: false` and change `message`, e.g. "Fully booked until March".
 * Leave `responseTime` or `location` empty ("") to hide that part of the second line.
 */
export const STATUS = {
  available: true,
  message: "Available for new projects",
  location: "Bangkok, GMT+7",
  responseTime: "Usually replies within a few hours",
};
